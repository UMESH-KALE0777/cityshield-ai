# apps/safe_route/routing.py
# ⚠️  This module is NOT used by views.py.
# The active route safety logic lives in apps/safe_route/views.py
# (calculate_safety_index via CrimeRecord area matching).
# Geopy/Nominatim geocoding was removed — too slow and rate-limited for prod.
